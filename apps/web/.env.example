# Clerk — must be prefixed VITE_ to be exposed to the browser bundle
# Same value as CLERK_PUBLISHABLE_KEY in apps/api/.env
VITE_CLERK_PUBLISHABLE_KEY="pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# API base URL
VITE_API_URL="http://localhost:3000"
